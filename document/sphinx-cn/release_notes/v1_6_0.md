# v1.6.0

**⭐️ 重要功能**

**✨ 次要功能和优化**

- ROS 2 插件
  - 新增 executor 线程调度策略、优先级及绑核配置
  - 新增对 ROS 2 Jazzy 的版本兼容支持：
    - 同时支持 Humble（rclcpp 16.x）与 Jazzy（rclcpp 28.x）
    - 通过 `rclcpp_VERSION_MAJOR` 区分 ROS 2 发行版
    - CI 工作流增加 Jazzy 测试矩阵
- 在 channel/rpc wrapper 中新增 index 字段用于唯一标识

**🐛Bug 修复**
