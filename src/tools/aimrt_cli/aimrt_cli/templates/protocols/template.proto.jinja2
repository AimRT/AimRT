{# This is the template file, do not Edit it! -#}
syntax = "proto3";

package {{ project_name }}.{{ proto.name }};

// define your messages here
message change_me {
    int32 num = 1;
}
{# blank line #}